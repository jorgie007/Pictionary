NOTE: Make sure a camera (internal or external) is connected to the computer

In order to start, do the following steps:
    -Install necessary packages stated in the report and below:
        -python version 3.7
        -Pygame (version 2.1.2, latest)
        -OpenCV (version 4.6.0, latest)
        -Mediapipe (version 0.9.0, latest)
        -Tensorflow (version 2.11.0, latest)
        -Numpy (version 1.21.6, latest)
    -Make sure you are in the same directory as this file (code directory)
    -run "python pictionary.py"